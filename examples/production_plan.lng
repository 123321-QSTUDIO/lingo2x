MODEL:
! 产品生产计划与设备能力分配问题;
! 决策变量：XA, XB, XC 分别为产品 A, B, C 的日产量;

! 目标函数：总利润最大;
MAX = 40 * XA + 30 * XB + 50 * XC;

! 设备能力约束;
[DEV_I]   2 * XA + 1 * XB + 2 * XC <= 60;   ! 设备I能力上限 60h;
[DEV_II]  1 * XA + 2 * XB + 1 * XC <= 40;   ! 设备II能力上限 40h;
[DEV_III] 0.5 * XA + 1.5 * XB + 0.5 * XC <= 50; ! 设备III能力上限 50h;

END
